# L2-MODULE: replace all of src/terraform/main.tf with this section.
module "resource_group" {
  source = "./modules/resource-group"

  name     = var.resource_group_name
  location = var.location
  tags     = var.tags
}
